<p id="notice"><%= notice %></p>

<p>
  <strong>Total:</strong>
  <%= @order.total %>
</p>

<%= link_to 'Back', orders_path %>
<%= link_to 'Authorize', order_authorize_path(@order) %>
<%= link_to 'Capture', order_capture_path(@order) %>
